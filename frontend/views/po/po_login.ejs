<!DOCTYPE html>
<html>
  <head>
    <%- include('../layout/head') -%>
    <title>출석하기 - <%= name %></title>
  </head>
  <body class="login">

    <div class="background">
      <div class="box">
        <div class="title">
          <h1><span><%= name %></span>의 <br/>출석하기<br/>사이트</h1>
        </div>
        <% if( rows[0].out_time==0 || rows[0].out_time==null ){ %>
        <div class="input">
          <form action="/po/insert" method="post">
            <input type="text" name="people_id" value="<%= people_id %>" style="display:none">
            <input type="text" name="name" value="<%= name %>" style="display:none">
            <% if(rows[0]){var tmp=rows[0].check_id} %>
            <input type="text" name="check_id" value="<%= tmp %>" style="display:none">
            <% if( rows[0].out_time==0 ){ %>
              <button id="upload"><span>출근</span></button>
            <% }else{ %>
              <button id="upload"><span>퇴근</span></button>
            <% } %>
          </form>
        </div>
        <% } %>
        <ul class="btn">
          <li><a href="/po/my/<%= people_id %>"><span>리스트보기</span></a></li>
          <li><a href="/po/sort/<%= name %>"><span>전체리스트</span></a></li>
        </ul>
        <p class="sign">
          <a href="/dev">by Sangho</a>
        </p>
      </div>
      <ul class="decription">
        <li>- 현재 사이트를 <span>즐겨찾기</span> 하셔서 사용하세요!</li>
        <li><hr /></li>
        <%- include('../layout/po_decription') -%>
      </ul>
    </div>

    <%- include('../layout/jquery_backstretch') -%>
    <script type="text/javascript">
    $(function(){
      // $( "#target1" ).click(function() {
      //   $.post("/insert",{cur_month:cur_month},function(rows){
      //     $("#view_list_div_2 div").html(rows);
      //   });
      // });
    });
    </script>
  </body>
</html>
